# 馬画像について

このゲームの馬は **CSSのみで描いたシルエット** で表示しています。
画像ファイル（PNG等）の読み込みは行っていないため、このフォルダに
ファイルを置いても表示には反映されません。

馬のデザインを変更したい場合は `css/style.css` の
`.horse-body` / `.horse-neck-head` / `.horse-mane` / `.horse-tail` /
`.horse-leg` / `.horse-eye` などのルールを編集してください。
